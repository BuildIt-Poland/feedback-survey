import styled from 'styled-components';

const LabelIcon = styled.label`
  :hover {
    cursor: pointer;
  }

  img {
    width: 8rem;
  }

  span {
    display: none;
  }
`;

export default LabelIcon;
